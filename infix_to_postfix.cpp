#include<stack>
#include<iostream>
using namespace std;

//assuming an operand is a single digit

int priority(string s)

void infixToPostfix(string inexp) {
    
    ptr=0;  // Points to start of string inexp
    
    while(ptr!=inexp.length()-1) {
        if
    }
}
